import React from "react";
import styles from './Previous.module.css'

const Previous = props => {
    return (
        <section id = "previousWorks" className={styles.prev}>Минулі роботи</section>
    );
}
export default Previous