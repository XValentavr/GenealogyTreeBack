import styles from './Guarantee.module.css'

const Guarantee = props => {
    return (
        <section id="guarantee" className={styles.guarantee}>Гарантії</section>
    );
}
export default Guarantee